logging:
  log_dir: "logs"

data:
  repo_root: "D:/code/mcfp"
  manifest_path: "D:/code/mcfp/data/manifests/stage1_manifest.jsonl"
  required_keys:
    - poses
    - labels
    - delta_pos
    - delta_rot
    - delta_mask

output:
  report_path: "D:/code/mcfp/data/reports/audit_stage1.json"
